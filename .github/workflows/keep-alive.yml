name: Keep Hugging Face Backend Awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          echo "Pinging Hugging Face backend..."
          curl -s -o /dev/null -w "HTTP Status: %{http_code}\n" https://anuragmishra3407-gridlock-backendmodel.hf.space/
          echo "Done!"
